import React from 'react';

// PropertiesPanel stub — detailed properties UI removed per request.
// Keep the component signature so imports elsewhere don't break.

const PropertiesPanel = ({ section, component, onUpdate, onDelete, customizations }) => {
  // Intentionally no UI: properties panel removed. Returning null so layout stays intact.
  return null;
};

export default PropertiesPanel;
